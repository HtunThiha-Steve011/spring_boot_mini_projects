spring:
  application:
    name: shop_smart
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/shop_smart
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true